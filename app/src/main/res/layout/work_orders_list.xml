<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".app.WorkOrderListActivity"
        android:orientation="vertical">

        <TextView
            android:id="@+id/work_orders_title"
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:paddingTop="5dp"
            android:text="@string/work_orders_title"
            android:background="@drawable/border_bottom"
            android:textAlignment="center" />

        <TextView
            android:id="@+id/no_assigned_work_orders"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="5dp"
            android:text="@string/no_assigned_work_orders"
            android:textAlignment="center"
            android:visibility="gone" />
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/work_orders_recyclerview"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        tools:listitem="@layout/work_order_item" >


                    </androidx.recyclerview.widget.RecyclerView>

                </LinearLayout>
            </layout>